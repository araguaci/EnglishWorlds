// Okay! so get the tabs by a class name and then check for the clicked one by the id and set the class to active by a switch statement
function SelectTab() {
  var tab = document.getElementById('PostTab');
  tab.className = "active";
  switch (expression) {
    case expression:
      break;
    default:
    }
  }
